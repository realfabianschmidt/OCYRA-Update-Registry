# Archive

This directory stores archived plans, retired notes, and historical snapshots that no longer belong in the active publish surface.

Rules:

- group archived files under a date folder such as `YYYY-MM-DD`
- keep active maintainer guidance in root docs or `docs/active/`
- do not move runtime-readable publish files into the archive
