# Registry Scripts

This directory contains repository-local guardrail scripts for the public plugin registry.

Current script set:

- `check-registry-structure.mjs`

Use these scripts before commit when you want to confirm that the repository still matches the intended publish surface.
